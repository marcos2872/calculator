import {
    Button,
    VerticalBox,
    HorizontalBox,
} from "std-widgets.slint";


export component AppWindow inherits Window {
    title: "Calculator";
    min-width: 500px;
    max-width: 500px;
    min-height: 700px;
    max-height: 700px;
    icon: @image-url("../assets/logo.png");
    background: #1e1e1e;
    // no-frame: true; // Comentado temporariamente - arrasto não funciona com no-frame no Linux
    in-out property <int> counter: 0;
    callback increment-counter();
    callback decrement-counter();
    callback minimize-window();
    callback close-window();

    VerticalBox {

        alignment: end;

        HorizontalBox {
            width: parent.width;
            spacing: 0px;
            padding: 0px;
            Button {
                width: parent.width / 4;
                height: 60px;
                text: "C";
                clicked => {
                    // root.decrement-counter();
                }
            }

            Button {
                width: parent.width / 4;
                height: 60px;
                text: "+/-";
                clicked => {
                    // root.increment-counter();
                }
            }

            Button {
                width: parent.width / 4;
                height: 60px;
                text: "%";
                clicked => {
                    // root.increment-counter();
                }
            }

            Button {
                width: parent.width / 4;
                height: 60px;
                text: "÷";
                clicked => {
                    // root.increment-counter();
                }
            }
        }
    }
}
